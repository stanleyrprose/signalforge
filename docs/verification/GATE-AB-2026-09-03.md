# Gate AB — v1.5 Production Compatibility Live Verification

Date (Asia/Yangon): 2026-09-03
Classification: `LIVE_SHORT`
Result: **PASS**

## Scope

Gate AB verifies that SignalForge v1.5 — Acquisition Policy & Local Contract Foundation changes the internal acquisition lifecycle without changing the validated v1.4.5 production topology, Worker boundary, scheduler cardinality, S13 business behavior, manual refresh contract, recovery semantics, or fleet placement.

## Releases and CI

- SignalForge production release: `618afaf4e6ef2ac7fdde64931f2a87dbc053a6f6`
- v1.4.5 known-good SignalForge rollback release: `d36f38336bf1b10580cffdb7fa96c7db119c2079`
- Worker Bangkok: `0a53558c9233622c69b083d61bed596cbedc0857`
- Worker Beijing: `0a53558c9233622c69b083d61bed596cbedc0857`
- Control Plane baseline remains `64ab3a907bb8a18808839176208023a5de976b55`; no Control Plane change was required by v1.5.
- PR #12 `feat: freeze and implement v1.5 local acquisition contract` merged to `main`.
- PR check `verify` completed successfully before merge.
- The live Bangkok `/srv/signalforge/active` symlink was verified to resolve to the exact v1.5 merge SHA above.

## Frozen before-state

The pre-implementation v1.4.5 production baseline recorded:

```text
canonical_items=16
signals=10
scheduler_runs=80
failed_runs=0
recovery_backlog=0
SignalForge health=GREEN
Bangkok Worker SignalForge application Runs=286
Bangkok timer=enabled/active/waiting
Beijing /srv/signalforge absent
```

Natural production polling continued between baseline capture and Gate AB live verification, so scheduler/Worker run counters were expected to increase. Business-state preservation is judged on canonical/signal integrity and expected additive acquisition rows, not on frozen scheduler counters.

## AB-1 — Gate Z cardinality unchanged — PASS

At the resumed live verification point, with the production timer intentionally paused for a controlled check:

```text
Worker SignalForge application Runs=291
SignalForge scheduler_runs=82
```

One reviewed `signalforge-run-due` dispatcher invocation was executed while S13 was not due.

Result:

```text
Worker SignalForge application Runs: 291 -> 292
SignalForge scheduler_runs:           82 -> 82
```

Therefore one scheduler-wrapper invocation still creates exactly one Worker operational Run, while no SignalForge business scheduler row is created when no source is due. AcquisitionRequest / AcquisitionAttempt remain SignalForge-internal and do not become Worker Runs.

After the timer was restored, systemd performed one immediate persistent-timer wrapper invocation. Worker SignalForge application Runs increased `293 -> 294` while `scheduler_runs` remained `83`, providing a second live observation of the same boundary.

## AB-2 — Gate AA / closed verb compatibility unchanged — PASS

The existing Control Plane dispatcher and manifest handshake were exercised through the reviewed SignalForge verbs.

- registered `signalforge-run-due` succeeded;
- registered `signalforge-refresh S13` succeeded;
- inactive source `signalforge-refresh S99` failed closed with exit `126` and `DENY: verb manifest incompatible: SignalForge source id is not active`;
- the rejected S99 request created no Worker application Run (`292 -> 292`).

No Worker ABI, Worker DB business schema, or Control Plane verb expansion was introduced by v1.5.

## AB-3 — Placement unchanged — PASS

Bangkok:

```text
/srv/signalforge/active
-> /srv/signalforge/releases/618afaf4e6ef2ac7fdde64931f2a87dbc053a6f6
```

Beijing:

```text
/srv/signalforge absent
```

The Beijing Control Plane also rejected `signalforge-status` with exit `126` and `DENY: SignalForge is Bangkok-only`.

## AB-4 — Timer unchanged — PASS

The timer was paused only for the controlled Gate AB checks and then restored through the reviewed `signalforge-resume` dispatcher verb.

Final state:

```text
signalforge-run-due.service: inactive/dead, last Result=success, ExecMainStatus=0
signalforge-run-due.timer:   enabled/active/waiting
```

The same systemd timer/service pair remains the production reliability backbone.

## AB-5 — S13 business behavior unchanged — PASS

The v1.5 production path retained S13 and Direct HTTP behavior. During Gate AB:

- `canonical_items` remained `16`;
- `signals` remained `10`;
- `failed_runs=0`;
- `recovery_backlog=0`;
- SignalForge health remained `GREEN`;
- S13 fetch, parse and recovery-backlog health remained `GREEN`;
- the live manual refresh created no duplicate or customer signal.

The CI/fixture regression suite covers the pre-existing baseline, unchanged/change parsing, health and recovery paths; no production recovery fault was manufactured solely for closure.

## AB-6 — Manual refresh unchanged — PASS

Invalid source test:

```text
signalforge-refresh S99
-> exit 126
-> denied before business execution
-> Worker application Runs 292 -> 292
```

Valid source test:

```text
signalforge-refresh S13
-> SUCCESS
-> trigger_kind=MANUAL
-> Worker application Runs 292 -> 293
-> SignalForge scheduler_runs 82 -> 83
-> acquisition_requests 6 -> 7
-> acquisition_attempts 6 -> 7
-> evidence_envelopes 6 -> 7
-> processing_records 6 -> 7
-> canonical_items 16 -> 16
-> signals 10 -> 10
```

This preserves the reviewed manual-refresh boundary and proves the v1.5 lifecycle rows are additive internal SignalForge state.

## AB-7 — Recovery semantics unchanged — PASS

Production recovery state remained clean:

```text
recovery_backlog=0
recovery_backlog_health=GREEN
```

The v1.5 CI/fixture suite retained the existing bounded recovery regression coverage. No crawl-all behavior, duplicate canonical row, or duplicate signal was observed in live operation.

## AB-8 — Fleet unchanged — PASS

Bangkok Worker:

```text
workerctl doctor=PASS
repo-contract=PASS
job-contracts=PASS
state-db=PASS
state-lock-wait=PASS
notification-backlog=PASS
```

Beijing Worker:

```text
workerctl doctor=PASS
repo-contract=PASS
job-contracts=PASS
state-db=PASS
state-lock-wait=PASS
notification-backlog=PASS
```

Final placement remains:

```text
Bangkok: Worker PASS / SignalForge ENABLED / GREEN
Beijing: Worker PASS / SignalForge DISABLED / zero application footprint
```

## AB-9 — Business-state preservation — PASS

Live SQLite verification after the valid manual refresh:

```text
PRAGMA quick_check=ok
canonical_items=16
signals=10
scheduler_runs=83
acquisition_requests=7
acquisition_attempts=7
evidence_envelopes=7
processing_records=7
```

The additive v1.5 tables are populated one-for-one for the exercised successful acquisition path, while pre-existing canonical and signal counts remain unchanged.

## Final production state

```text
SignalForge release=618afaf4e6ef2ac7fdde64931f2a87dbc053a6f6
SignalForge health=GREEN
S13 fetch_health=GREEN
S13 parse_health=GREEN
S13 recovery_backlog_health=GREEN
canonical_items=16
signals=10
failed_runs=0
recovery_backlog=0
signalforge-run-due.timer=enabled/active/waiting
Bangkok Worker doctor=PASS
Beijing Worker doctor=PASS
Beijing SignalForge footprint=ABSENT
```

## Gate conclusion

Gate AB is **PASS**.

SignalForge v1.5 is now production-complete for its authorized P0 scope: the local acquisition policy/request/attempt/evidence/processing contract is live on Bangkok, v1.4.5 remains the known-good rollback baseline, and the externally validated Worker/Control/Fleet/S13 behavior is preserved.

Remote Provider, Beijing acquisition, Mac production acquisition, Browser/Browserless, Redis/Celery, distributed queue, automatic cross-zone failover and speculative Webhook capability remain outside v1.5 and are not triggered by this closure.
