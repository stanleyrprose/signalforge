# Gate Z — SignalForge Single-scheduler Invocation Live Verification

Date (Asia/Yangon): 2026-09-03
Classification: `LIVE_SHORT`
Result: **PASS**

## Releases

- SignalForge: `ef02915663eb6d5f9f70208eeaf07c28a07874da`
- Worker runtime/provider: `32caaed78e7e8557bd260c932a57e84745522c79`
- Control Plane closed verbs: `584e38ea4fc1a73817f878fcdcf5d64cf2efe91d`

## Acceptance and live result

Bangkok `signalforge-run-due.service` runs as `User=signalforge`, `Group=signalforge` in `worker-signalforge.slice`, with `ExecStart=/srv/signalforge/active/bin/signalforge run-due`. It does not use Generic `worker@.service`; Worker DB has zero `job_id='signalforge'` rows.

`systemctl list-unit-files 'signalforge*'` exposes only `signalforge-run-due.service` and one `signalforge-run-due.timer`; no S13/per-source production timer exists.

With the production timer paused, one closed `signalforge-run-due` invocation changed Worker `application='signalforge'` operational Run count exactly `21 -> 22`. S13 was not due, so SignalForge business scheduler rows remained `6 -> 6`.

The six existing business scheduler rows have six distinct `worker_run_id` values and all six correlations exist in Worker DB. Correlated Worker rows contain `application=signalforge`, `job_id=NULL`, `app_job_id=NULL`, `app_trigger_id=NULL`, while business `source_id`, application scheduler run id and trigger identity remain in SignalForge DB. Worker `runs` has no SignalForge `source_id` or application `app_run_id` columns.

A live ACL probe as the `signalforge` UID attempted to open `/srv/worker/state/worker.db` with `O_WRONLY` and received `PermissionError`, errno 13; `os.access(..., W_OK)` was false.

Beijing strict-zero verification passed: `/srv/signalforge` is absent, `signalforge-run-due.service` is `LoadState=not-found`, the active SignalForge application descriptor is absent, and no `signalforge` system user exists.

After verification, `signalforge-resume` restored the persistent timer. Its missed-due catch-up completed as `RECONCILIATION`, `changed=0`, `signals_created=0`, `backlog_remaining=0`, `status=SUCCESS`. Final SignalForge status is PASS with `canonical_items=16`, `signals=10`, `recovery_backlog=0`, timer `enabled/active/waiting`; Bangkok and Beijing `workerctl doctor` both PASS.

## Gate conclusion

Gate Z is **PASS**. SignalForge uses one dedicated application scheduler envelope rather than one Worker job/timer per source. One scheduler invocation creates one Worker operational Run, business/source Jobs remain application-owned in SignalForge DB, Worker state keeps opaque correlation only, SignalForge cannot write Worker state, and Beijing remains strict zero-footprint.
