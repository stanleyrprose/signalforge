# Gate O — SignalForge Topology / MPT Fixture Live Verification

Date (Asia/Yangon): 2026-09-02
Classification: `CI_AUTOMATED + LIVE_SHORT`
Result: **PASS**

## Exact releases

- SignalForge application: `7f89994a699c89b9f727f526ffe676401b4eea9f`
- Worker provider/runtime: `32caaed78e7e8557bd260c932a57e84745522c79`
- Control Plane closed verbs: `584e38ea4fc1a73817f878fcdcf5d64cf2efe91d`

## Gate O contract

Gate O requires:

1. SignalForge installed on Bangkok only;
2. Beijing SignalForge absent;
3. Source Registry owned by the SignalForge repository;
4. a real Bangkok MPT fixture;
5. parser/canonical tests;
6. first baseline creates no customer signal;
7. Direct HTTP is the production-first engine;
8. SignalForge business/scheduler Run correlates to the Worker operational Run.

All eight conditions passed.

## Source discovery and Direct HTTP

The 2026-09-02 Bangkok audit found that the public Tender landing page returned an empty content body even though historic `tablepress-319` CSS remained. WordPress REST probing was not accepted (`403`). Browser escalation was therefore not assumed.

A simpler issuer-owned Direct HTTP path was found:

```text
https://mpt.com.mm/wp-sitemap.xml
  -> https://mpt.com.mm/page-sitemap.xml
  -> MPT English Tender detail pages with lastmod
```

Normal TLS verification remained enabled. No `verify=False`, `curl -k`, third-party renderer, Playwright, or Crawlee path was used.

The SignalForge-owned `registry/Source-Registry-v1.yaml` records S13 as:

```text
engine = direct_http
network_zone = myanmar-international
canonical_node = bangkok
browser_production_approved = false
first_baseline_customer_signal = false
```

## Real MPT parser fixture

The first audited detail page was fetched from Bangkok through normal TLS:

```text
https://mpt.com.mm/en/purchasing-of-top-up-card-with-qr-code-4/
```

The real page structure exposes:

```text
Date          = July 17, 2026
Reference No  = CCO-2026-001
Project Name  = Purchasing of Top-up Card with QR code
Location      = Yangon,Myanmar
Deadline      = 2026-08-06
```

A minimized parser-relevant fixture was committed in `tests/fixtures/mpt_tender_detail.html`, with the original captured response SHA recorded in the fixture comment.

Focused tests before merge:

```text
python -m unittest -v tests.test_contract tests.test_mpt tests.test_engine
7 tests -> PASS

python -m compileall -q signalforge tests
-> PASS

python -m json.tool registry/Source-Registry-v1.yaml
-> PASS

sh -n deploy/deploy-signalforge-release.sh
sh -n bin/signalforge
-> PASS
```

GitHub Actions `verify` passed for SignalForge PR #1 before squash merge.

## Bangkok-only deployment

SignalForge exact release `7f89994a699c89b9f727f526ffe676401b4eea9f` was deployed to Bangkok only:

```text
/srv/signalforge/active
  -> /srv/signalforge/releases/7f89994a699c89b9f727f526ffe676401b4eea9f
```

Before Gate O closure:

```text
signalforge-run-due.timer = disabled / inactive
```

This ensured the first real collection was an explicit controlled Gate O run rather than an unattended timer run.

Beijing verification:

```text
/srv/signalforge = absent
signalforge-run-due.service LoadState = not-found
```

The Control Plane also rejects SignalForge operations on Beijing:

```text
signalforge-status -> exit 126
DENY: SignalForge is Bangkok-only
```

## Closed Control Plane verbs

Control Plane PR #10 added only these reviewed operations:

```text
signalforge-status
signalforge-run-due
signalforge-pause
signalforge-resume
```

The GitHub workflow requires `target=bangkok`; the server-side forced-command dispatcher independently requires the Bangkok/nanobot host. The operations expose only the reviewed SignalForge service/timer and do not create an arbitrary `systemctl` surface.

Both CI checks for PR #10 passed before squash merge as `584e38ea4fc1a73817f878fcdcf5d64cf2efe91d`.

## Live correlation permission defect and fix

The first live `signalforge-run-due` correctly failed closed before business collection.

Observed:

```text
ExecStartPre prepare-application = SUCCESS
ExecStart SignalForge            = FAILED
WorkerContextError               = Permission denied
path                              = /run/worker/apps/signalforge/<INVOCATION_ID>.json
```

The per-application directory and descriptor were already isolated correctly, but ancestor `/run/worker/apps` was `0750 root:root`, preventing the dedicated `signalforge` UID from traversing to its own descriptor.

Worker PR #44 fixed only this traversal boundary:

```text
/run/worker/apps                   0711 root:root
/run/worker/apps/signalforge       0750 root:signalforge
<INVOCATION_ID>.json               0440 root:signalforge
```

`0711` allows traversal but not directory listing; cross-application content remains protected by each application directory/group.

Focused provider regression:

```text
python -m unittest -v tests.test_r5_provider
3 tests -> PASS
python tools/verify_repo.py
-> PASS
```

GitHub Actions passed and PR #44 squash-merged as:

```text
32caaed78e7e8557bd260c932a57e84745522c79
```

Both Worker nodes were upgraded to that exact release. SignalForge's service envelope was then reinstalled on Bangkok so `ExecStartPre/ExecStopPost` pinned the new Worker runtime.

## First successful real baseline

Immediately before the successful controlled baseline:

```text
canonical_items = 0
scheduler_runs  = 0
signals         = 0
```

The run was entered through the closed Control Plane operation:

```text
signalforge-run-due
```

Result:

```text
source_id         = S13
status            = SUCCESS
baseline          = 1
canonical_items   = 6
changed           = 6
scheduler_runs    = 1
signals_created   = 0
signals total     = 0
source last_error = null
```

This proves the first baseline created evidence/canonical state while producing **zero customer-visible signals**.

## Canonical MPT records observed

The six canonical records were all structural MPT Tender pages, not normal consumer pages:

```text
CCO-2026-001      | Purchasing of Top-up Card with QR code
202606-CTO-031    | New DG replacement project FY26
COO-3-2026        | Security Service in Yangon and Nay Pyi Taw
202606-CTO-030    | PSTN and FTTH last mile Maintenance at Yangon and Naypyitaw
COO-1-2026        | Car Rental Service for Hi-ace_2026 (Yangon)
202602-CTO-027    | Core and Important Site DG Maintenance Project FY26
```

All six raw HTML evidence files exist under the S13 evidence root. Observed evidence sizes were approximately 150 KB each, and their SHA-256 values are stored in `canonical_items.evidence_sha256`.

## Worker Run correlation

SignalForge scheduler Run:

```text
app_run_id       = 20c304ed-ceb2-4038-a6d9-cfd3745380f2
source_id        = S13
status           = SUCCESS
baseline         = 1
signals_created  = 0
worker_run_id    = signalforge-20260902T150147Z-080ce1d8
```

The Worker operational database contains the same Run ID:

```text
run_id        = signalforge-20260902T150147Z-080ce1d8
application   = signalforge
job_id        = null
worker        = bangkok
status        = SUCCESS
error_type    = null
runtime_sha   = 32caaed78e7e8557bd260c932a57e84745522c79
```

Result: **business/scheduler Run ↔ Worker operational Run correlation verified**.

## Post-run health

Bangkok:

```text
workerctl doctor = PASS
nanobot-gateway.service = active
```

Beijing:

```text
workerctl doctor = PASS
hermes-gateway.service = active
/srv/signalforge = absent
SignalForge unit = not-found
```

The first failed live attempt did not contaminate SignalForge baseline state: no source scheduler Run, canonical item, or customer signal had been created before the permission fix.

## Gate conclusion

Gate O is **PASS**.

R5 now has a Bangkok-only, Direct HTTP, issuer-sitemap-driven MPT pipeline with real evidence, structural parsing, baseline suppression, and exact Worker operational correlation. Browser/Crawlee production remains unapproved and unnecessary for S13.

Per the PRD sequence, the next action after this evidence is merged is to enable `signalforge-run-due.timer` through the closed `signalforge-resume` operation, beginning MPT 24x7 polling with the durable 15-minute source cadence.
