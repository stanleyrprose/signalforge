#!/bin/sh
set -eu
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
export PYTHONPATH="$ROOT${PYTHONPATH:+:$PYTHONPATH}"
export SIGNALFORGE_REPO_ROOT="$ROOT"
exec /usr/bin/python3 -m signalforge.cli "$@"
